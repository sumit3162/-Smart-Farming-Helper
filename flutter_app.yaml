name: smart_farming_helper
description: AI-powered farming assistant

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  tflite_flutter: ^0.9.0
  http: ^0.13.3

flutter:
  assets:
    - assets/crop_model.tflite